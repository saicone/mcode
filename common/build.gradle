dependencies {
    compileOnly libs.gson
    compileOnly libs.nbt
}

javadoc {
    options {
        links "https://javadoc.io/doc/com.google.code.gson/gson/${libs.gson.get().version}/"
        links 'https://javadoc.saicone.com/nbt/'
    }
}